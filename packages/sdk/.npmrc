# NPM Publishing Configuration
# registry=https://registry.npmjs.org/
# access=public

# 如果使用私有 registry，取消注释并配置
# registry=https://your-private-registry.com/

# 确保发布公开包（对于 scoped packages）
# @luckdb:registry=https://registry.npmjs.org/

