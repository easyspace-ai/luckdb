# NPM Publishing Configuration

# 发布到 npm 官方源（发布时必须使用官方源）
registry=https://registry.npmjs.org/

# 发布为公开包（对于 scoped packages @luckdb/sdk）
access=public

# 如果使用私有 registry，修改上面的 registry 配置
# registry=https://your-private-registry.com/

# 国内镜像源（日常开发可以使用，但发布时必须用官方源）
# registry=https://registry.npmmirror.com/  # 淘宝镜像
# registry=https://mirrors.cloud.tencent.com/npm/  # 腾讯云镜像

